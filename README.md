# gcc-num-format - Floating point number formatting routines.

Can format numbers using ENG, or SCI notation.

Use 'make test' or 'make verify' to test.

# To Do

* Fix display of fixed point numbers so that they are rounded up when possible and not formatted using scientific notation.
